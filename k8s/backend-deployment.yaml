apiVersion: apps/v1
kind: Deployment

metadata:
  name: backend

spec:
  replicas: 1

  selector:
    matchLabels:
      app: backend

  template:
    metadata:
      labels:
        app: backend

    spec:
      containers:
      - name: backend
        image: 385105852446.dkr.ecr.ap-south-1.amazonaws.com/expense-backend:latest

        ports:
        - containerPort: 8000